# Python / venv
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Build-Artefakte (GitHub Pages nutzt docs/, lokal serve.sh nutzt dist/)
dist/
.*.staging/
.*.bak/
latentmol-quiz-pages.zip

# RDKit-Galerie-Cache im Projektroot (docs/gallery/ gehört ins Repo!)
/gallery/

# OS / Editor
.DS_Store
Thumbs.db
.cursor/

# Nicht für Web-Quiz — nur lokal behalten
cards/
catalog_assets/
desktop/
print/
causality_summary.png
catalog_*.pdf
catalog_*.tex
*.aux
*.log
*.synctex.gz

# Daten: nur Inference-JSONs fürs Build nötig
Daten/*
!Daten/inference_*.json

# Interne Planung / Langfassung (optional lokal, nicht im Repo)
WEBAPP_REQUIREMENTS.md
WISSENSCHAFTLICHE_EINORDNUNG.full.md
